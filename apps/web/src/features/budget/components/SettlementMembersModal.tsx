import{useEffect,useState}from'react';import{X}from'lucide-react';import type{BudgetMember}from'../types'
export function SettlementMembersModal({eligible,current,saving,onSave,onClose}:{eligible:BudgetMember[];current:BudgetMember[];saving:boolean;onSave:(ids:string[])=>Promise<void>;onClose:()=>void}){const[selected,setSelected]=useState(()=>current.filter(x=>x.active).map(x=>x.id));const[error,setError]=useState<string|null>(null);useEffect(()=>{const close=(e:KeyboardEvent)=>{if(e.key==='Escape'&&!saving)onClose()};window.addEventListener('keydown',close);return()=>window.removeEventListener('keydown',close)},[onClose,saving]);async function save(){if(selected.length<2){setError('Wybierz co najmniej 2 osoby.');return}try{await onSave(selected);onClose()}catch(r){setError(r instanceof Error?r.message:'Nie udało się zapisać.')}}return <div className="fixed inset-0 z-[80] grid place-items-end bg-black/75 sm:place-items-center sm:p-5"><section role="dialog" aria-modal="true" className="surface w-full rounded-t-3xl p-5 sm:max-w-lg sm:rounded-3xl"><header className="flex justify-between"><div><h2 className="text-xl font-semibold">Osoby rozliczające wydatki</h2><p className="mt-1 text-xs text-brand-muted">Zmiana nie wpływa na historyczne snapshoty.</p></div><button onClick={onClose} aria-label="Zamknij"><X/></button></header><div className="mt-5 space-y-2">{eligible.map(m=><label key={m.id} className="flex items-center justify-between rounded-xl border border-white/10 p-3"><span>{m.displayName}<span className="ml-2 text-xs text-brand-muted">{m.role}</span></span><input type="checkbox" checked={selected.includes(m.id)} onChange={e=>setSelected(v=>e.target.checked?[...v,m.id]:v.filter(x=>x!==m.id))} className="accent-[#d4af37]"/></label>)}</div>{error?<p className="mt-3 text-sm text-red-300">{error}</p>:null}<button disabled={saving} onClick={()=>void save()} className="mt-5 w-full rounded-xl bg-brand-gold px-4 py-3 font-semibold text-black">{saving?'Zapisywanie…':'Zapisz osoby'}</button></section></div>}
