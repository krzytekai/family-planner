import{useEffect}from'react'
export function DeleteBudgetModal({title,body,deleting,onDelete,onClose}:{title:string;body:string;deleting:boolean;onDelete:()=>Promise<void>;onClose:()=>void}){useEffect(()=>{const f=(e:KeyboardEvent)=>{if(e.key==='Escape'&&!deleting)onClose()};window.addEventListener('keydown',f);return()=>window.removeEventListener('keydown',f)},[deleting,onClose]);return <div className="fixed inset-0 z-[90] grid place-items-center bg-black/75 p-4"><section role="dialog" aria-modal="true" className="surface w-full max-w-md rounded-3xl p-5"><h2 className="text-lg font-semibold">{title}</h2><p className="mt-2 text-sm text-brand-muted">{body}</p><p className="mt-2 text-sm text-red-300">Tej operacji nie można cofnąć.</p><div className="mt-5 flex justify-end gap-2"><button disabled={deleting} onClick={onClose} className="rounded-xl border border-white/10 px-4 py-2.5">Anuluj</button><button disabled={deleting} onClick={()=>void onDelete().then(onClose)} className="rounded-xl bg-red-500/80 px-4 py-2.5 font-semibold text-white">{deleting?'Usuwanie…':'Usuń'}</button></div></section></div>}
