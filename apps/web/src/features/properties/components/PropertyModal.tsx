import{useState,type FormEvent}from'react';import{X}from'lucide-react';import{entries,propertyUnitTypeLabels}from'../property-labels';import type{Property,PropertyInput,PropertyUnitType,UnitInput}from'../types'
export function PropertyModal({familyId,properties,mode,editing,saving,onProperty,onUnit,onClose}:{familyId:string;properties:Property[];mode:'property'|'unit';editing?:Property|null;saving:boolean;onProperty:(i:PropertyInput,id?:string)=>Promise<void>;onUnit:(i:UnitInput)=>Promise<void>;onClose:()=>void}){const[name,setName]=useState(editing?.name??'');const[address,setAddress]=useState(editing?.address??'');const[description,setDescription]=useState(editing?.description??'');const[propertyId,setPropertyId]=useState(properties[0]?.id??'');const[type,setType]=useState<PropertyUnitType>('apartment');async function submit(e:FormEvent){e.preventDefault();if(mode==='property')await onProperty({familyId,name,address,description},editing?.id);else await onUnit({familyId,propertyId,name,type});onClose()}return <div className="fixed inset-0 z-[90] grid place-items-end bg-black/70 sm:place-items-center sm:p-5"><section role="dialog" aria-modal="true" className="surface w-full rounded-t-3xl p-5 sm:max-w-md sm:rounded-3xl"><header className="flex items-center justify-between"><h2 className="font-semibold">{editing?'Edytuj nieruchomość':mode==='property'?'Nowa nieruchomość':'Nowa część nieruchomości'}</h2><button onClick={onClose} aria-label="Zamknij" className="grid h-11 w-11 place-items-center"><X className="h-4 w-4"/></button></header><form onSubmit={e=>void submit(e)} className="mt-4 space-y-3">{mode==='unit'?<label className="block text-xs text-brand-muted">Nieruchomość<select required value={propertyId} onChange={e=>setPropertyId(e.target.value)} className="mt-1 w-full rounded-xl border border-white/10 bg-[#13131b] px-3 py-3 text-brand-text">{properties.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label>:null}<label className="block text-xs text-brand-muted">Nazwa<input required maxLength={120} value={name} onChange={e=>setName(e.target.value)} className="mt-1 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-brand-text"/></label>{mode==='property'?<><label className="block text-xs text-brand-muted">Adres opcjonalny<input value={address} onChange={e=>setAddress(e.target.value)} className="mt-1 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3"/></label><label className="block text-xs text-brand-muted">Opis opcjonalny<textarea value={description} onChange={e=>setDescription(e.target.value)} className="mt-1 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3"/></label></>:<label className="block text-xs text-brand-muted">Typ<select value={type} onChange={e=>setType(e.target.value as PropertyUnitType)} className="mt-1 w-full rounded-xl border border-white/10 bg-[#13131b] px-3 py-3">{entries(propertyUnitTypeLabels).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label>}<button disabled={saving||mode==='unit'&&!propertyId} className="min-h-11 w-full rounded-xl bg-brand-gold font-semibold text-black disabled:opacity-50">{saving?'Zapisywanie…':'Zapisz'}</button></form></section></div>}
