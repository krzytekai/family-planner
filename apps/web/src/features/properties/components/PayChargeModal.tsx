import { formatDateTimeLocal, parseDateTimeLocal } from '../../../lib/date-time-local'
import{useState,type FormEvent}from'react';import{X}from'lucide-react';import type{PaymentInput,PropertyCharge}from'../types'
export function PayChargeModal({familyId,charge,defaultAmount,saving,onSave,onClose}:{familyId:string;charge:PropertyCharge;defaultAmount:number|null;saving:boolean;onSave:(i:PaymentInput)=>Promise<void>;onClose:()=>void}){const[amount,setAmount]=useState(defaultAmount===null?'':String(defaultAmount/100));const[paidAt,setPaidAt]=useState(()=>formatDateTimeLocal(new Date()));const[notes,setNotes]=useState(charge.notes??'');const[syncBudget,setSyncBudget]=useState(false);async function submit(e:FormEvent){e.preventDefault();await onSave({familyId,chargeId:charge.id,amountCents:Math.round(Number(amount.replace(',','.'))*100),paidAt:parseDateTimeLocal(paidAt).toISOString(),notes,syncBudget});onClose()}return <div className="fixed inset-0 z-[90] grid place-items-end bg-black/75 sm:place-items-center sm:p-5"><section role="dialog" aria-modal="true" className="surface w-full rounded-t-3xl p-5 sm:max-w-md sm:rounded-3xl"><header className="flex items-center justify-between"><h2 className="font-semibold">Oznacz jako zapłacone</h2><button onClick={onClose} aria-label="Zamknij" className="grid h-11 w-11 place-items-center"><X className="h-4 w-4"/></button></header><form onSubmit={e=>void submit(e)} className="mt-4 space-y-3"><label className="block text-xs text-brand-muted">Kwota<input required inputMode="decimal" value={amount} onChange={e=>setAmount(e.target.value)} className="mt-1 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3"/></label><label className="block text-xs text-brand-muted">Data płatności<input required type="datetime-local" value={paidAt} onChange={e=>setPaidAt(e.target.value)} className="mt-1 w-full rounded-xl border border-white/10 bg-[#13131b] px-3 py-3"/></label><label className="block text-xs text-brand-muted">Notatka<textarea value={notes} onChange={e=>setNotes(e.target.value)} className="mt-1 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3"/></label><label className="flex items-center gap-2 text-xs"><input type="checkbox" checked={syncBudget} onChange={e=>setSyncBudget(e.target.checked)}/>Dodaj do Budżetu jako wydatek</label><button disabled={saving} className="min-h-11 w-full rounded-xl bg-brand-green font-semibold text-black disabled:opacity-50">{saving?'Zapisywanie…':'Zapłacone'}</button></form></section></div>}
