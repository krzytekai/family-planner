import{getSupabaseClient}from'../../lib/supabase';import{resolveApiUrl}from'../../lib/api-url'
export async function deletePlatformUser(targetUserId:string){const{data}=await getSupabaseClient()!.auth.getSession(),token=data.session?.access_token;if(!token)throw new Error('Sesja wygasła.');let response:Response;try{response=await fetch(resolveApiUrl('/api/admin/platform-users'),{method:'DELETE',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({targetUserId})})}catch{throw new Error('Nie można połączyć się z serwerem administracyjnym.')}const payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error(payload.error??'Nie udało się usunąć konta.');return payload as{ok:true;alreadyDeleted?:boolean}}
