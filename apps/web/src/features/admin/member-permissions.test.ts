import{describe,expect,it}from'vitest';import{canManageFamilyMember}from'./member-permissions';describe('family member management hierarchy',()=>{it.each([['owner','admin'],['owner','adult'],['owner','child'],['admin','adult'],['admin','child']]as const)('%s can manage %s',(actor,target)=>expect(canManageFamilyMember(actor,target)).toBe(true));it.each([['owner','owner'],['admin','owner'],['admin','admin'],['adult','adult'],['adult','child'],['child','adult']]as const)('%s cannot manage %s',(actor,target)=>expect(canManageFamilyMember(actor,target)).toBe(false))})
