import { BellOff, CheckCheck, ChevronRight, Clock3, X } from 'lucide-react'
import { formatNotificationDate, groupNotifications } from '../notification-utils'
import type { AppNotification, NotificationPreferences, Reminder } from '../types'
import { NotificationPreferences as PreferencesForm } from './NotificationPreferences'

interface Props { notifications: AppNotification[]; reminders: Reminder[]; preferences: NotificationPreferences; loading: boolean; saving: boolean; error: string | null; onOpen: (notification: AppNotification) => void; onToggleRead: (notification: AppNotification) => void; onMarkAllRead: () => void; onEditReminder: (reminder: Reminder) => void; onDeleteReminder: (id: string) => void; onPreferences: (value: NotificationPreferences) => void; onClose: () => void }
const sections = [{ key: 'new', label: 'Nowe' }, { key: 'today', label: 'Dzisiaj' }, { key: 'earlier', label: 'Wcześniejsze' }] as const

export function NotificationCenter(props: Props) {
  const groups = groupNotifications(props.notifications)
  return <div className="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm" onMouseDown={props.onClose}><aside role="dialog" aria-modal="true" aria-label="Centrum powiadomień" onMouseDown={(event) => event.stopPropagation()} className="ml-auto h-full w-full max-w-lg overflow-y-auto border-l border-white/10 bg-brand-bg p-4 shadow-2xl sm:p-6"><header className="flex items-center justify-between"><div><p className="text-xs uppercase tracking-[.16em] text-brand-gold">Centrum</p><h2 className="text-xl font-semibold">Powiadomienia</h2></div><button type="button" onClick={props.onClose} aria-label="Zamknij" className="rounded-xl p-2 text-brand-muted hover:bg-white/5"><X className="h-5 w-5"/></button></header><button type="button" disabled={props.saving || props.notifications.every((item) => item.readAt)} onClick={props.onMarkAllRead} className="mt-4 inline-flex items-center gap-2 rounded-xl border border-brand-gold/20 px-3 py-2 text-xs text-brand-gold disabled:opacity-40"><CheckCheck className="h-4 w-4"/>Oznacz wszystkie jako przeczytane</button>{props.error ? <p role="alert" className="mt-4 rounded-xl border border-red-400/15 bg-red-400/5 p-3 text-xs text-red-300">{props.error}</p> : null}{props.loading ? <div className="mt-5 h-40 animate-pulse rounded-2xl bg-white/[.03]"/> : null}{!props.loading && props.notifications.length === 0 ? <div className="mt-5 rounded-2xl border border-white/5 p-8 text-center"><BellOff className="mx-auto h-6 w-6 text-brand-muted"/><p className="mt-3 text-sm">Brak powiadomień</p></div> : null}{!props.loading ? sections.map((section) => groups[section.key].length ? <section key={section.key} className="mt-6"><h3 className="text-xs font-semibold uppercase tracking-[.14em] text-brand-muted">{section.label}</h3><div className="mt-2 space-y-2">{groups[section.key].map((item) => <article key={item.id} className={`rounded-2xl border p-3 ${item.readAt ? 'border-white/5 bg-white/[.015]' : 'border-brand-gold/15 bg-brand-gold/[.035]'}`}><button type="button" onClick={() => props.onOpen(item)} className="flex w-full items-start gap-3 text-left"><span className={`mt-1 h-2 w-2 shrink-0 rounded-full ${item.readAt ? 'bg-white/15' : 'bg-brand-gold'}`}/><span className="min-w-0 flex-1"><span className="block text-sm font-medium">{item.title}</span>{item.body ? <span className="mt-1 block text-xs text-brand-muted">{item.body}</span> : null}<span className="mt-2 block text-[10px] text-brand-muted">{formatNotificationDate(item.createdAt)}</span></span><ChevronRight className="mt-1 h-4 w-4 text-brand-muted"/></button><button type="button" disabled={props.saving} onClick={() => props.onToggleRead(item)} className="mt-2 text-[10px] text-brand-gold hover:underline">{item.readAt ? 'Oznacz jako nieprzeczytane' : 'Oznacz jako przeczytane'}</button></article>)}</div></section> : null) : null}<section className="mt-8 border-t border-white/5 pt-6"><h3 className="text-sm font-semibold">Nadchodzące przypomnienia</h3>{props.reminders.length === 0 ? <p className="mt-2 text-xs text-brand-muted">Nie masz aktywnych przypomnień.</p> : <div className="mt-3 space-y-2">{props.reminders.map((item) => <div key={item.id} className="rounded-xl border border-white/5 p-3"><p className="text-sm">{item.title ?? 'Przypomnienie'}</p><p className="mt-1 inline-flex items-center gap-1 text-[11px] text-brand-muted"><Clock3 className="h-3 w-3"/>{formatNotificationDate(item.remindAt)}</p><div className="mt-2 flex gap-3 text-[10px]"><button type="button" onClick={() => props.onEditReminder(item)} className="text-brand-gold">Zmień</button><button type="button" onClick={() => props.onDeleteReminder(item.id)} className="text-red-300">Usuń</button></div></div>)}</div>}</section><div className="mt-8 border-t border-white/5 pt-6"><PreferencesForm value={props.preferences} saving={props.saving} onChange={props.onPreferences}/></div></aside></div>
}
