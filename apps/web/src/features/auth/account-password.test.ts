import{readFileSync}from'node:fs';import{resolve}from'node:path';import{describe,expect,it}from'vitest';const account=readFileSync(resolve(process.cwd(),'src/features/auth/AccountModal.tsx'),'utf8'),app=readFileSync(resolve(process.cwd(),'src/app/App.tsx'),'utf8'),header=readFileSync(resolve(process.cwd(),'src/components/AppHeader.tsx'),'utf8');describe('self password change',()=>{it('is available from the shared account control to every authenticated role',()=>{expect(header).toContain('aria-label="Moje konto"');expect(app).toContain('<AccountModal');expect(app).not.toContain('family.role===<AccountModal')});it('uses the signed-in Supabase session without admin credentials',()=>{expect(account).toContain('auth.updateUser({password})');expect(account).not.toContain('service_role');expect(account).not.toContain('updateUserById')})})
