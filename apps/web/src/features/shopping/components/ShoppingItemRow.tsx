import { Check, Pencil, Trash2, UserRound } from 'lucide-react'
import type { FamilyRole } from '../../../types/domain'
import { canDeleteShoppingItem, canEditShoppingItem, formatQuantity } from '../shopping-utils'
import type { ShoppingItem } from '../types'

interface Props { item: ShoppingItem; userId: string; role: FamilyRole; updating: boolean; onToggle: (item: ShoppingItem) => void; onEdit: (item: ShoppingItem) => void; onDelete: (item: ShoppingItem) => void }
export function ShoppingItemRow({ item, userId, role, updating, onToggle, onEdit, onDelete }: Props) {
  const canEdit = canEditShoppingItem(item, userId, role); const canDelete = canDeleteShoppingItem(item, userId, role); const quantity = formatQuantity(item.quantity, item.unit)
  return <article className={`rounded-xl border border-white/[.05] bg-black/10 px-2 py-1 sm:rounded-2xl sm:p-4 ${item.isPurchased ? 'opacity-60' : ''}`}><div className="flex items-start gap-1 sm:gap-3"><button type="button" disabled={updating} onClick={() => onToggle(item)} aria-label={item.isPurchased ? `Cofnij kupienie: ${item.name}` : `Oznacz jako kupione: ${item.name}`} className="grid h-11 w-11 shrink-0 place-items-center rounded-xl disabled:cursor-wait"><span className={`grid h-[26px] w-[26px] place-items-center rounded-lg border transition ${item.isPurchased ? 'border-brand-green bg-brand-green text-black' : 'border-white/20 bg-black/10 hover:border-brand-gold/50'}`}>{updating ? <span className="h-3.5 w-3.5 animate-spin rounded-full border-2 border-current border-t-transparent"/> : item.isPurchased ? <Check className="h-4 w-4"/> : null}</span></button><div className="min-w-0 flex-1"><div className="flex min-w-0 items-start justify-between"><h3 className={`min-w-0 break-words pt-1 text-sm font-medium leading-tight sm:text-base ${item.isPurchased ? 'line-through' : ''}`}>{item.name}</h3><div className="flex shrink-0">{canEdit ? <button type="button" onClick={() => onEdit(item)} aria-label={`Edytuj produkt: ${item.name}`} className="grid h-10 w-10 place-items-center rounded-lg text-brand-muted/80 hover:bg-white/5 hover:text-brand-gold"><Pencil className="h-4 w-4"/></button> : null}{canDelete ? <button type="button" onClick={() => onDelete(item)} aria-label={`Usuń produkt: ${item.name}`} className="grid h-10 w-10 place-items-center rounded-lg text-brand-muted/80 hover:bg-red-400/10 hover:text-red-300"><Trash2 className="h-4 w-4"/></button> : null}</div></div><div className="mobile-metadata -mt-0.5 flex flex-wrap items-center gap-x-1.5 gap-y-0.5 leading-tight text-brand-muted/75">{quantity ? <span className="font-medium text-brand-text/70">{quantity}</span> : null}{item.category ? <span className="text-brand-gold/80">{item.category}</span> : null}<span className="inline-flex items-center gap-0.5"><UserRound className="h-2.5 w-2.5"/>Dodał(a): {item.createdBy.displayName}</span>{item.isPurchased ? <span className="text-brand-green/85">Kupił(a): {item.purchasedBy?.displayName ?? 'użytkownik'}{item.purchasedAt ? ` • ${new Intl.DateTimeFormat('pl-PL', { day: '2-digit', month: '2-digit', hour: '2-digit', minute: '2-digit' }).format(new Date(item.purchasedAt))}` : ''}</span> : null}</div>{item.note ? <p className="mt-0.5 text-[11px] leading-snug text-brand-muted/85 sm:text-sm">{item.note}</p> : null}</div></div></article>
}
