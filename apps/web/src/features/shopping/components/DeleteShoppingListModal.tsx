import { useEffect, useState } from 'react'
import { AlertTriangle, Trash2, X } from 'lucide-react'
import type { ShoppingList } from '../types'
interface Props { list: ShoppingList; deleting: boolean; onDelete: (list: ShoppingList) => Promise<void>; onClose: () => void }
export function DeleteShoppingListModal({ list, deleting, onDelete, onClose }: Props) { const [error, setError] = useState<string | null>(null); useEffect(() => { const close = (event: KeyboardEvent) => { if (event.key === 'Escape' && !deleting) onClose() }; window.addEventListener('keydown', close); return () => window.removeEventListener('keydown', close) }, [deleting, onClose]); async function confirm() { setError(null); try { await onDelete(list); onClose() } catch (reason) { setError(reason instanceof Error ? reason.message : 'Nie udało się usunąć listy.') } } return <div className="fixed inset-0 z-[90] grid place-items-end bg-black/75 backdrop-blur-sm sm:place-items-center sm:p-6"><section role="alertdialog" aria-modal="true" aria-labelledby="delete-shopping-list-title" className="surface w-full rounded-t-3xl p-5 sm:max-w-lg sm:rounded-3xl sm:p-6"><header className="flex justify-between"><AlertTriangle className="h-10 w-10 rounded-xl bg-red-400/10 p-2 text-red-300"/><button type="button" disabled={deleting} onClick={onClose} aria-label="Zamknij" className="p-2 text-brand-muted"><X className="h-5 w-5"/></button></header><h2 id="delete-shopping-list-title" className="mt-5 text-xl font-semibold">Usunąć listę „{list.name}”?</h2><p className="mt-2 text-sm leading-relaxed text-brand-muted">Usunięte zostaną również wszystkie produkty z tej listy.<br/>Tej operacji nie można cofnąć.</p>{error ? <p role="alert" className="mt-4 text-sm text-red-300">{error}</p> : null}<div className="mt-6 flex flex-col-reverse gap-2 sm:flex-row sm:justify-end"><button type="button" disabled={deleting} onClick={onClose} className="rounded-xl border border-white/10 px-4 py-2.5 text-sm text-brand-muted">Anuluj</button><button type="button" disabled={deleting} onClick={() => void confirm()} className="inline-flex items-center justify-center gap-2 rounded-xl bg-red-500/15 px-4 py-2.5 text-sm font-semibold text-red-200 disabled:cursor-wait">{deleting ? <span className="h-4 w-4 animate-spin rounded-full border-2 border-current border-t-transparent"/> : <Trash2 className="h-4 w-4"/>}{deleting ? 'Usuwanie…' : 'Usuń listę'}</button></div></section></div> }
