import { useEffect, useMemo, useRef, useState } from 'react'
import { Archive, ArchiveRestore, ListFilter, Pencil, Plus, ShoppingCart, Trash2 } from 'lucide-react'
import type { FamilyContext } from '../../../types/domain'
import { canCreateShoppingList, canManageShoppingList, filterShoppingItems, shoppingCounts, sortShoppingItems } from '../shopping-utils'
import type { ShoppingFilter, ShoppingItem, ShoppingItemInput, ShoppingList, ShoppingListInput } from '../types'
import { useShopping } from '../hooks/useShopping'
import { DeleteShoppingItemModal } from './DeleteShoppingItemModal'
import { DeleteShoppingListModal } from './DeleteShoppingListModal'
import { ShoppingItemModal } from './ShoppingItemModal'
import { ShoppingItemRow } from './ShoppingItemRow'
import { ShoppingListModal } from './ShoppingListModal'
import { ShoppingListSelector } from './ShoppingListSelector'

const filters: Array<{ value: ShoppingFilter; label: string }> = [{ value: 'all', label: 'Wszystkie' }, { value: 'unpurchased', label: 'Do kupienia' }, { value: 'purchased', label: 'Kupione' }, { value: 'mine', label: 'Moje' }]
interface Props { family: FamilyContext; quickAddRequest: number }

export function ShoppingView({ family, quickAddRequest }: Props) {
  const shopping = useShopping(family.familyId)
  const [showArchived, setShowArchived] = useState(false); const [filter, setFilter] = useState<ShoppingFilter>('all'); const [category, setCategory] = useState('')
  const [listEditor, setListEditor] = useState<ShoppingList | 'new' | null>(null); const [itemEditor, setItemEditor] = useState<ShoppingItem | 'new' | null>(null)
  const [deleteList, setDeleteList] = useState<ShoppingList | null>(null); const [deleteItem, setDeleteItem] = useState<ShoppingItem | null>(null)
  const handledRequest = useRef(quickAddRequest); const canCreateList = canCreateShoppingList(family.role)
  useEffect(() => { if (quickAddRequest !== handledRequest.current) { handledRequest.current = quickAddRequest; if (shopping.selectedList && !shopping.selectedList.isArchived) setItemEditor('new'); else if (canCreateList) setListEditor('new') } }, [canCreateList, quickAddRequest, shopping.selectedList])
  const categories = useMemo(() => [...new Set(shopping.items.map((item) => item.category).filter((value): value is string => Boolean(value)))].sort(), [shopping.items])
  const visibleItems = useMemo(() => sortShoppingItems(filterShoppingItems(shopping.items, filter, family.userId, category)), [category, family.userId, filter, shopping.items])
  const unpurchased = visibleItems.filter((item) => !item.isPurchased); const purchased = visibleItems.filter((item) => item.isPurchased); const counts = shoppingCounts(shopping.items)
  const canManageList = shopping.selectedList ? canManageShoppingList(shopping.selectedList, family.userId, family.role) : false
  async function saveList(input: ShoppingListInput, id?: string) { if (id) await shopping.updateList(id, input); else await shopping.createList(input) }
  async function saveItem(input: ShoppingItemInput, id?: string) { if (id) await shopping.updateItem(id, input); else await shopping.createItem(input) }

  if (shopping.loadingLists) return <div className="mx-auto max-w-[1500px] p-4 md:p-7"><div className="surface h-[32rem] animate-pulse rounded-2xl" aria-label="Ładowanie list zakupów"/></div>
  if (shopping.error) return <div className="mx-auto max-w-[1500px] p-4 md:p-7"><p role="alert" className="surface rounded-2xl p-5 text-red-300">Nie udało się pobrać zakupów: {shopping.error}</p></div>

  return <div className="mx-auto max-w-[1500px] p-4 md:p-7"><header className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-1 text-xs uppercase tracking-[.18em] text-brand-gold">{family.familyName}</p><h1 className="text-2xl font-semibold md:text-3xl">Lista zakupów</h1><p className="mt-1 text-sm text-brand-muted">Wiele rodzinnych list, wspólne dodawanie i wygodne zakupy.</p></div>{canCreateList ? <button type="button" onClick={() => setListEditor('new')} className="inline-flex items-center justify-center gap-2 rounded-xl border border-brand-gold/25 px-4 py-2.5 text-sm text-brand-gold"><Plus className="h-4 w-4"/>Nowa lista</button> : null}</header>{shopping.actionError ? <p role="alert" className="mt-4 rounded-xl border border-red-400/15 bg-red-400/5 p-3 text-sm text-red-300">{shopping.actionError}</p> : null}{shopping.lists.length === 0 ? <section className="surface mt-6 rounded-2xl p-10 text-center"><div className="mx-auto grid h-14 w-14 place-items-center rounded-full bg-brand-gold/10 text-brand-gold"><ShoppingCart className="h-7 w-7"/></div><h2 className="mt-4 text-lg font-semibold">Nie masz jeszcze żadnej listy zakupów.</h2><p className="mt-2 text-sm text-brand-muted">{canCreateList ? 'Utwórz pierwszą listę i zacznij dodawać produkty.' : 'Poproś dorosłego o utworzenie listy.'}</p>{canCreateList ? <button type="button" onClick={() => setListEditor('new')} className="mt-5 rounded-xl bg-brand-gold px-5 py-2.5 text-sm font-semibold text-black">Utwórz pierwszą listę</button> : null}</section> : <div className="mt-6 grid gap-4 lg:grid-cols-[260px_1fr]"><ShoppingListSelector lists={shopping.lists} selectedId={shopping.selectedListId} showArchived={showArchived} onSelect={shopping.selectList} onShowArchived={setShowArchived}/><main className="min-w-0">{shopping.selectedList ? <><section className="surface rounded-2xl p-4 sm:p-5"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><div className="flex items-center gap-2"><h2 className="text-xl font-semibold">{shopping.selectedList.name}</h2>{shopping.selectedList.isArchived ? <span className="rounded-full bg-white/5 px-2 py-1 text-[10px] text-brand-muted">Archiwalna</span> : null}</div>{shopping.selectedList.description ? <p className="mt-1 text-sm text-brand-muted">{shopping.selectedList.description}</p> : null}<p className="mt-2 text-xs text-brand-muted">{counts.unpurchased} do kupienia • {counts.purchased} kupione</p></div><div className="flex flex-wrap gap-2">{!shopping.selectedList.isArchived ? <button type="button" onClick={() => setItemEditor('new')} className="inline-flex items-center gap-2 rounded-xl bg-brand-gold px-4 py-2.5 text-sm font-semibold text-black"><Plus className="h-4 w-4"/>Dodaj produkt</button> : null}{canManageList ? <><button type="button" onClick={() => setListEditor(shopping.selectedList)} aria-label="Edytuj listę" className="rounded-xl border border-white/10 p-2.5 text-brand-muted"><Pencil className="h-4 w-4"/></button><button type="button" onClick={() => void shopping.archiveList(shopping.selectedList!.id, !shopping.selectedList!.isArchived).catch(() => undefined)} aria-label={shopping.selectedList.isArchived ? 'Przywróć listę' : 'Archiwizuj listę'} className="rounded-xl border border-white/10 p-2.5 text-brand-muted">{shopping.selectedList.isArchived ? <ArchiveRestore className="h-4 w-4"/> : <Archive className="h-4 w-4"/>}</button><button type="button" onClick={() => setDeleteList(shopping.selectedList)} aria-label="Usuń listę" className="rounded-xl border border-red-400/10 p-2.5 text-red-300"><Trash2 className="h-4 w-4"/></button></> : null}</div></div></section><section className="mt-4 flex flex-col gap-3 sm:flex-row sm:items-center"><div className="scrollbar-none flex gap-2 overflow-x-auto" role="group" aria-label="Filtry produktów">{filters.map((value) => <button key={value.value} type="button" aria-pressed={filter === value.value} onClick={() => setFilter(value.value)} className={`shrink-0 rounded-full border px-3 py-2 text-xs ${filter === value.value ? 'border-brand-gold/30 bg-brand-gold text-black' : 'border-white/10 text-brand-muted'}`}>{value.label}</button>)}</div>{categories.length ? <label className="flex items-center gap-2 text-xs text-brand-muted"><ListFilter className="h-4 w-4"/><select value={category} onChange={(event) => setCategory(event.target.value)} className="rounded-xl border border-white/10 bg-[#101017] px-3 py-2"><option value="">Wszystkie kategorie</option>{categories.map((value) => <option key={value}>{value}</option>)}</select></label> : null}</section>{shopping.loadingItems ? <div className="surface mt-4 h-72 animate-pulse rounded-2xl" aria-label="Ładowanie produktów"/> : visibleItems.length === 0 ? <div className="surface mt-4 rounded-2xl p-9 text-center"><p className="font-medium">Brak produktów dla tego filtra.</p><p className="mt-1 text-sm text-brand-muted">Dodaj produkt lub wybierz inny filtr.</p></div> : <div className="mt-4 grid items-start gap-4 xl:grid-cols-2"><section className="surface rounded-2xl p-3"><h3 className="px-2 py-2 text-xs font-semibold uppercase tracking-[.16em] text-brand-gold">Do kupienia ({unpurchased.length})</h3><div className="space-y-3">{unpurchased.length ? unpurchased.map((item) => <ShoppingItemRow key={item.id} item={item} userId={family.userId} role={family.role} updating={shopping.updatingIds.has(item.id)} onToggle={(value) => void shopping.togglePurchased(value)} onEdit={setItemEditor} onDelete={setDeleteItem}/>) : <p className="py-8 text-center text-xs text-brand-muted">Wszystko kupione.</p>}</div></section><section className="surface rounded-2xl p-3"><h3 className="px-2 py-2 text-xs font-semibold uppercase tracking-[.16em] text-brand-green">Kupione ({purchased.length})</h3><div className="space-y-3">{purchased.length ? purchased.map((item) => <ShoppingItemRow key={item.id} item={item} userId={family.userId} role={family.role} updating={shopping.updatingIds.has(item.id)} onToggle={(value) => void shopping.togglePurchased(value)} onEdit={setItemEditor} onDelete={setDeleteItem}/>) : <p className="py-8 text-center text-xs text-brand-muted">Brak kupionych produktów.</p>}</div></section></div>}</> : <div className="surface rounded-2xl p-8 text-center text-sm text-brand-muted">Włącz „Pokaż archiwalne” lub wybierz aktywną listę.</div>}</main></div>}{listEditor ? <ShoppingListModal familyId={family.familyId} list={listEditor === 'new' ? null : listEditor} saving={shopping.saving} onSave={saveList} onClose={() => setListEditor(null)}/> : null}{itemEditor && shopping.selectedList ? <ShoppingItemModal familyId={family.familyId} listId={shopping.selectedList.id} item={itemEditor === 'new' ? null : itemEditor} saving={shopping.saving} onSave={saveItem} onClose={() => setItemEditor(null)}/> : null}{deleteItem ? <DeleteShoppingItemModal item={deleteItem} deleting={shopping.deletingIds.has(deleteItem.id)} onDelete={shopping.deleteItem} onClose={() => setDeleteItem(null)}/> : null}{deleteList ? <DeleteShoppingListModal list={deleteList} deleting={shopping.deletingIds.has(deleteList.id)} onDelete={shopping.deleteList} onClose={() => setDeleteList(null)}/> : null}</div>
}
